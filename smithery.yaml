# Smithery configuration file: https://smithery.ai/docs/build/project-config

# TODO: consider switching to single config line: 'runtime: "python"'
runtime: python
# build:
#   dockerfile: Dockerfile
#   dockerBuildPath: .
# startCommand:
#   type: http  # TODO: verify whether stdio is still supported
#   configSchema:
#     # JSON Schema defining the configuration options for the MCP.
#     type: object
#     required:
#       - notesDir
#       - database
#     properties:
#       logLevel:
#         type: string
#         default: INFO
#       notesDir:
#         type: string
#         default: ""
#         description: >-
#           Where the notes are stored.

#           Blank stores under `./data/notes` relative path to mcp working
#           directory.
#       database:
#         type: string
#         default: ""
#         description: >-
#           SQLite file path or SQLAlchemy URL for the index database.

#           Leave blank to use `./data/db/zettelkasten.db` relative to the MCP
#           working directory.

#           PostgreSQL, MySQL, and SQL Server are supported via SQLAlchemy
#           connection URLs, e.g. `postgresql+psycopg2://user:password@host:port/dbname`
#   exampleConfig:
#     logLevel: INFO
#     notesDir: /data/notes
#     database: /data/db/zettelkasten.db
