# Smithery configuration file: https://smithery.ai/docs/build/project-config
#
# When using @smithery.server() decorator with config_schema, the Python
# Pydantic model defines the configuration schema automatically.
# The smithery.yaml just needs to point to the factory function.

runtime: python
